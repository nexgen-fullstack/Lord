# The built site IS the deployable artefact and is committed on purpose:
# GitHub Pages serves this repo as-is, with no build step of its own.
node_modules/
npm-debug.log*
.DS_Store
Thumbs.db

# Android app: build scratch and the finished APK are regenerated by
# `npm run apk`. The signing key must never be published.
android/build/
android/dist/
android/keystore/
